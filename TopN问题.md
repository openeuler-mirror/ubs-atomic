# 安全编码TOPN问题规范清单
## 一、安全编码TOPN问题判断原则
### 1. 一般判断原则
能被外部触发的CVSS得分大于0分小于7分的如下7类安全问题，才判断是否属于安全编码TOPN观察项，“外部”主要指来自用户面、管理面、数据面的输入数据。

### 2. 例外情况
符合一般判断原则，但不属于安全编码TOPN观察项的场景：
- 只有应用系统的特殊权限用户才能触发的拒绝服务问题，不属于安全编码TOPN观察项

### 3. 补充解读
1. 不能被外部触发的设备内部板间通信存在的问题不算TOPN观察项
2. 在TOPN观察项中不对开源第三方组件漏洞做出要求
3. 避开业务系统，通过后台未越权修改了配置文件，触发的问题不属于TOPN观察项

## 二、安全编码TOPN问题明细清单
### 1. 跨站脚本
- **TOPN观察项**：Web应用服务端在处理或存储来自客户端、第三方系统、外部接口等外部输入数据前未进行正确校验（包括未校验和校验不当）、其后在输出到客户端前也未正确进行相应的编码或转义，导致反射型或存储型XSS。
- **核心场景**：
  1. 来自Web客户端的不可信数据包括但不限于：text、password、textareas、file表单域、hidden fields、selection boxes、check boxes、radio buttons、cookies、HTTP headers、热点链接包含的URL参数
  2. 不可信数据做为页面的一部分输出到客户端时，由于未做编码而被外部触发导致的跨站脚本问题
- **排查参考**：《Web安全：XSS共性问题排查指导_V1.1.docx》
- **历史问题数**：
  - 2017考核年总数：43（致命+严重：4；一般+提示：39）
  - 2016考核年总数：37（致命+严重：15；一般+提示：22）
  - 2015考核年总数：228（致命+严重：22；一般+提示：208）
- **备注**：
  1. 对于红线范围内的问题按红线要求执行。红线2.0要求：使用Appscan工具扫描发现相关的告警，如果确认是问题的，都必须在版本发布前得到解决。
  2. 在业务系统中不能触发的跨站脚本问题不属于TOPN观察项
- **优先级**：中
- **排查细则**：仅限web，排查方法参考对应排查指导文档

### 2. SQL注入
- **TOPN观察项**：WEB应用程序、C/C++应用、Andriod应用(自研代码)中，使用来自客户端、第三方系统、外部接口等外部输入数据构造SQL语句访问数据库时，导致SQL注入问题。
- **核心场景**：
  1. 对来自外部可控的数据未做有效的校验
  2. 使用动态拼装SQL语句，而没有使用参数化查询；应该优先考虑使用参数化查询来防止SQL注入
- **排查参考**：SQL注入共性问题排查指导
- **历史问题数**：
  - ICSL：2017考核年总数16（致命+严重0；一般+提示16）、2016考核年总数15（致命+严重3；一般+提示12）、2015考核年总数55（致命+严重20；一般+提示35）
  - CSEC：2017年2个、2016年2个
- **备注**：
  1. 对于红线范围内的问题按红线要求执行。红线2.0要求：使用Appscan工具扫描发现相关的告警，如果确认是问题的，都必须在版本发布前得到解决。
  2. 不包括Andriod原生态SQL注入问题；Andriod自研代码中编码类SQL注入问题列入TOP观察项。
  3. 直接登录服务后端业务数据面，数据库管理员用户使用产品组件的维护管理工具导致的SQL注入，不属于TOPN观察项
- **优先级**：高
- **排查细则**：
  - 通用关键词：sql|insert|update|delete|select
  - java语言关键词：
    1. 非预编译导致的SQL注入 ，关键字：Statement 、.execute 、.executeQuery、jdbcTemplate、queryForInt、queryForObject、queryForMap、getConnection等
    2. 过滤不完整导致SQL注入，关键字：Statement 、execute 、jdbcTemplate、queryForInt、queryForObject、queryForMap、executeQuery、getConnection等
    3. 不正确使用预编译导致的SQL注入 ，关键字：PreparedStatement
    4. Hibernate框架下的SQL注入，关键字：org.hibernate
    5. iBatis/MyBatis 框架下的SQL注入，iBatis关键字：import com.ibatis；MyBatis关键字：org.apache.ibatis
    6. BME平台下的SQL注入，DAS关键字：com.huawei.bme.das

### 3. 缓冲区溢出
#### 3.1 常规缓冲区溢出
- **TOPN观察项**：C/C++代码使用用户输入、通信协议、用户态输入（对于内核程序而言）等外部输入数据，直接进行缓冲区操作、或作为数组下标、或作为地址偏移等，导致缓冲区写/读越界、或写任意内存地址。
- **核心场景**：
  1. 设备处理接口协议报文时、未检查协议字段长度是否超过目的缓冲区大小、就直接使用危险函数拷贝数据到目的缓冲区
  2. 设备接收用户输入的命令，未检查命令长度，造成命令写越界
  3. 内核代码在直接或间接处理由系统调用传递过来的串口、上层应用等外部输入数据时，直接进行缓冲区操作。例如：ioctl、read、write、mmap等系统调用
  4. 使用用户输入的数据作为读写内存中固定地址的偏移值，并进行读写
  5. 双机备份心跳包中传递的主机状态值在数组合法值域外，系统直接使用传入的状态值来索引状态的字符串数组，并打印日志
- **历史问题数**：
  - ICSL：2017考核年总数261（致命+严重24；一般+提示237）、2016考核年总数102（致命+严重15；一般+提示87）、2015考核年总数68（致命+严重20；一般+提示48）
  - CSEC：2017年58个、2016年10个
- **优先级**：低
- **排查细则**：无具体排查方法

#### 3.2 安全函数使用错误导致的缓冲区溢出
- **TOPN观察项**：能被外部触发的由于安全函数使用错误导致的缓冲区溢出问题
- **核心场景**：
  1. 使用了宏封装的安全函数，只使用一个长度参数
  2. 使用了函数封装安全函数，只使用一个长度参数
  3. 使用安全函数时destMax参数来自外部输入，存在超过dest缓冲区实际大小的情况
  4. 使用scanf_s系列函数时，长度参数超过输出缓冲区大小
- **排查参考**：安全函数错误用法共性问题排查指导
- **历史问题数**：
  - ICSL：2017考核年总数33（致命+严重1；一般+提示32）、2016考核年总数8（致命+严重1；一般+提示7）
  - CSEC：2017年17个、2016年4个
- **优先级**：高
- **排查细则**：
  1. 仅涉及自研且C/C++语言
  2. 是否有问题，以对应安全编程规范标准确认

### 4. 未正确清理特定内容
- **TOPN观察项**：系统使用用户输入、通信协议、用户态输入、第三方系统、外部接口等外部输入数据时,未正确进行合法性校验，导致系统宕机、复位、CPU/内存资源过度消耗导致拒绝服务、服务或进程不可用
- **核心场景（仅符合以下情况可算TOPN观察项）**：
  1. 外部输入数据未做合法性校验或校验不正确引起内存泄漏，导致的拒绝服务
  2. 使用外部协议报文输入作为循环条件，或者作为循环条件的判断依据，引起死循环
  3. 外部作为内存分配大小参数、导致可申请超大内存，导致的拒绝服务
  4. Android应用对不可信Intent传入的数据造成不合法操作，导致拒绝服务攻击
- **历史问题数**：
  - ICSL：2017考核年总数132（致命+严重3；一般+提示129）、2015考核年总数122（致命+严重23；一般+提示99）、2016考核年总数56（致命+严重0；一般+提示56）
  - CSEC：2017年2个、2016年7个
- **备注**：未正确清理特定内容是指没有正确过滤和释义用户可控输入中的特定内容，导致对软件的行为和完整性产生不良影响，其他问题场景不在该问题类别范围
- **优先级**：中
- **排查细则**：
  - 范围：有外部输入的
  - 具体范围：外部输入，未做合理判断的，作为循环条件，开辟内存的
  - 关键字：无

### 5. 引用空指针
- **TOPN观察项**：使用指针前未进行非空校验，出现空指针引用，且能被外部触发导致系统宕机/复位、应用拒绝服务等问题。
- **核心场景**：
  1. C/C++代码对内存分配函数的返回值没有检查，直接访问该指针会导致系统异常
  2. Android代码中跨信任边界传入的Intent未判空，出现空指针导致应用异常
- **历史问题数**：
  - ICSL：2017考核年总数172（致命+严重1；一般+提示171）、2016考核年总数35（致命+严重0；一般+提示35）、2015考核年总数40（致命+严重0；一般+提示40）
  - CSEC：2017年2个
- **优先级**：高
- **排查细则**：
  - 范围：全覆盖
  - 关键字：malloc/calloc/realloc/strdup/getline/getdelim等，其他指针操作无法列举

### 6. 命令注入
- **TOPN观察项**：程序执行由用户输入、接口数据报文、操作系统环境变量、普通用户可修改的配置文件等外部数据所构建的全部或部分命令语句，导致实际执行结果与预期不一致，产生命令注入。
- **核心场景**：
  1. C/C++代码中使用未经校验的用户输入数据作为system()/popen()函数的参数，而system()/popen()是通过调用命令解析器（如UNIX/Linux的bash、sh，Windows的CMD.exe）来执行入参指定的程序/命令，导致命令注入
  2. Java代码中使用Runtime.exec()函数来调用系统命令解析器来执行用户输入的命令或参数
- **排查参考**：命令注入共性问题排查指导
- **历史问题数**：
  - ICSL：2017考核年总数87（致命+严重45；一般+提示42）、2016考核年总数43（致命+严重22；一般+提示21）、2015考核年总数31（致命+严重21；一般+提示10）
  - CSEC：2017年1个
- **备注**：
  1. 对于红线范围内的问题按红线要求执行。红线2.0要求：对于web应用，使用Appscan工具扫描发现的相关告警，如果确认是问题的，都必须在版本发布前得到解决。
  2. 直接登录服务后端业务数据面，数据库管理员用户使用产品组件的维护管理工具产生的命令注入，不属于TOPN观察项
- **优先级**：高
- **排查细则**：
  - 范围：外部输入拼接命令的
  - 关键字：以对应排查指导文档为主

### 7. 整数溢出或回绕
- **TOPN观察项**：能够被用户输入、通信协议、用户态输入（对于内核程序而言）、第三方系统、外部接口等外部输入触发的运算表达式整数溢出或回绕、符号位扩展，导致缓冲区溢出、死循环、溢出的结果作为内存分配大小使用等问题（包括外部输入数据直接或间接参与运算的场景）。
- **核心场景**：
  1. 数值变量加减乘除运算
  2. 变量自加自减运算
  3. 指针偏移运算、作为数组的边界
  4. 其他运算符参见《C＆C++语言安全编程规范》的整数安全章节
- **历史问题数**：
  - ICSL：2017考核年总数46（致命+严重3；一般+提示43）、2016考核年总数27（致命+严重1；一般+提示26）
  - CSEC：2017年22个、2016年9个
- **优先级**：低
- **排查细则**：
  - 范围：外部输入作为运算表达式
  - 关键字：无