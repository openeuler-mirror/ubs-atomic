# 安全排查报告

**项目**: ubs-atomic  
**日期**: 2026-05-29  
**依据**: TopN问题.md 中定义的7类安全问题  
**范围**: 全代码仓（src/、include/）

---

## 一、跨站脚本（XSS）— ✅ 无风险

**结论**: 不适用。本仓库为纯 C/C++ 底层系统库（共享内存队列+分布式锁），无任何 Web 页面渲染或 HTML/JavaScript 输出。

---

## 二、SQL注入 — ✅ 无风险

**结论**: 不适用。代码中没有使用 SQL 数据库操作。

---

## 三、缓冲区溢出

### 3.1 常规缓冲区溢出

#### 🔴 风险点 1：`enqueue_local` 整数溢出绕过 size 检查

- **文件**: [MPSCRingBuffer.cpp:L410](file:///home/david/workspace/ubs-atomic/src/ub_comm_queue/MPSCRingBuffer.cpp#L410)
- **代码**:
  ```cpp
  uint32_t total_len = MSG_HEADER_LEN + body_len;
  if (__builtin_expect(total_len > max_msg_size_, 0))
      return -EMSGSIZE;
  ```
- **问题**: `MSG_HEADER_LEN`(16) + `body_len`(uint32_t) 在 `body_len` 接近 `UINT32_MAX` 时溢出为小值，绕过 size 检查，导致后续 `memcpy` 写入远超缓冲区的数据。
- **攻击路径**: 外部通过 `ub_comm_queue_send()` API 传入构造的 `body_length` 即可触发。
- **修复**: 使用 `size_t` 进行加法以避免溢出。

#### 🔴 风险点 2：`enqueue_remote` 相同问题

- **文件**: [MPSCRingBuffer.cpp:L489](file:///home/david/workspace/ubs-atomic/src/ub_comm_queue/MPSCRingBuffer.cpp#L489)
- **代码**:
  ```cpp
  uint32_t total_len = MSG_HEADER_LEN + body_len;
  if (__builtin_expect(total_len > max_size, 0))
      return -EMSGSIZE;
  ```
- **问题**: 与 `enqueue_local` 完全相同的整数溢出问题。
- **修复**: 使用 `uint64_t` 进行加法以避免溢出。

#### 🟡 风险点 3：`dequeue` 整数溢出导致数据截断

- **文件**: [MPSCRingBuffer.cpp:L583](file:///home/david/workspace/ubs-atomic/src/ub_comm_queue/MPSCRingBuffer.cpp#L583)
- **代码**:
  ```cpp
  uint32_t real_len = sizeof(message_header_t) + hdr->body_length;
  uint32_t copy_len = (real_len > buffer_cap) ? buffer_cap : real_len;
  ```
- **问题**: `sizeof(message_header_t)`(size_t) + `hdr->body_length`(uint32_t) 溢出后 `real_len` 变为小值，导致数据截断读取，可能引发下游消息头解析异常。
- **修复**: 使用 `size_t` 进行计算。

### 3.2 安全函数使用错误 — ✅ 安全

所有日志格式化函数（`snprintf`、`vsnprintf`）均正确传入缓冲区大小。

---

## 四、未正确清理特定内容

### 4.1 内存泄漏

#### 动态内存释放 — ✅ 已正确处理

- [message_manage.cpp:L61-L64](file:///home/david/workspace/ubs-atomic/src/ub_lock/message_manage.cpp#L61-L64): `create_message()` 返回值在调用处（[L104](file:///home/david/workspace/ubs-atomic/src/ub_lock/message_manage.cpp#L104)、[L124](file:///home/david/workspace/ubs-atomic/src/ub_lock/message_manage.cpp#L124)）被 `MessagePtr`（`unique_ptr<message_t, MessageDeleter>`）封装，RAII 确保释放。
- [ub_mutex_lock.cpp:L398-L400](file:///home/david/workspace/ubs-atomic/src/ub_lock/ub_mutex_lock.cpp#L398-L400): `create_mutex_message()` 同样在调用处（[L462](file:///home/david/workspace/ubs-atomic/src/ub_lock/ub_mutex_lock.cpp#L462)）被 `MessagePtr` 封装。

**结论**: 内存管理良好，无泄漏风险。

### 4.2 死循环/无限循环 — ✅ 安全

排查了所有 `while(true)` 循环：

| 位置 | 退出机制 | 状态 |
|------|---------|------|
| `enqueue_local` CAS重试 | `MAX_CAS_RETRIES` (1000) | ✅ 安全 |
| `enqueue_remote` CAS重试 | `MAX_CAS_RETRIES` (1000) | ✅ 安全 |
| `SpinLock::lock_init` | CAS成功或READY状态 | ✅ 安全 |
| `SpinLock::lock` | deadline超时返回TIMEOUT | ✅ 安全 |
| `force_release_to_bitmap` | CAS成功或条件满足 | ✅ 安全 |
| `wake_one_mutex_local_waiter` | sanity上限 (UB_MAX_CAPACITY*2) | ✅ 安全 |
| `wake_one_waiter` | sanity上限 (MUTEX_WAKE_SCAN_LIMIT) | ✅ 安全 |

### 4.3 超大内存分配

#### 🟡 `CalculateMemorySize` 无上限校验

- **文件**: [MPSCRingBuffer.cpp:L68-L75](file:///home/david/workspace/ubs-atomic/src/ub_comm_queue/MPSCRingBuffer.cpp#L68-L75)
- **问题**: `capacity` 和 `max_msg_size` 仅校验了非零和2的幂次，缺少上限限制，极端值下 `single_entry_size * capacity` 可能溢出。
- **修复**: 在 `check_memory_capacity` 中增加上限校验。

---

## 五、引用空指针

### 5.1 API 入口判空保护 — ✅ 合格

所有对外 API 入口均进行了 `nullptr` 检查：
- `ub_comm_queue_send` → 检查 `handle`, `*handle`, `msg`
- `ub_comm_queue_recv` → 检查 `handle`, `*handle`, `buffer`
- `UBShmTransport::send` → 检查 `msg`
- `ub_spin_lock`/`ub_spin_unlock` → 检查 `lock`, `location`
- `ub_mutex_lock`/`ub_mutex_unlock` → 检查 `lock`, `location`

### 5.2 Share Memory Placement New — 🟢 有防护

- [UBShmTransport.cpp:L546](file:///home/david/workspace/ubs-atomic/src/ub_comm_queue/UBShmTransport.cpp#L546): placement new 前有 try-catch 验证共享内存可访问性。

### 5.3 位图操作边界保护 — ✅ 安全

`set_shared_owner_bitmap` 和 `clear_shared_owner_bitmap` 均有 `process_id >= 32` 边界检查。

---

## 六、命令注入 — ✅ 无风险

**结论**: 生产代码中无 `system()`、`popen()`、`exec()` 等命令执行函数调用。Test 目录下的 `system()` 调用仅用于测试环境。

---

## 七、整数溢出或回绕

| 位置 | 问题 | 风险 | 状态 |
|------|------|------|------|
| `enqueue_local` L410 | `MSG_HEADER_LEN + body_len` | 🔴 高 | **已修复** |
| `enqueue_remote` L489 | `MSG_HEADER_LEN + body_len` | 🔴 高 | **已修复** |
| `dequeue` L583 | `sizeof(hdr) + body_length` | 🟡 中 | **已修复** |
| `CalculateMemorySize` L75 | 无上限校验 | 🟡 中 | **已修复** |
| `try_inc_global_ref` | 溢出前检查 `ref >= INT32_MAX` | ✅ 安全 | 无需修复 |
| `set_shared_owner_bitmap` | `(1u << process_id)` 有边界检查 | ✅ 安全 | 无需修复 |

---

## 修复总结

| 序号 | 文件 | 问题类型 | 风险等级 | 修复措施 |
|------|------|---------|---------|---------|
| 1 | MPSCRingBuffer.cpp | 整数溢出→缓冲区溢出 | 🔴 高 | `enqueue_local`: 使用 `size_t` 避免加法溢出 |
| 2 | MPSCRingBuffer.cpp | 整数溢出→缓冲区溢出 | 🔴 高 | `enqueue_remote`: 使用 `uint64_t` 避免加法溢出 |
| 3 | MPSCRingBuffer.cpp | 整数溢出→数据截断 | 🟡 中 | `dequeue`: 使用 `size_t` 计算 `real_len` |
| 4 | UBShmTransport.cpp | 超大内存分配 | 🟡 中 | `check_memory_capacity`: 增加 `capacity` 和 `max_msg_size` 上限校验 |